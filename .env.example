# Variables de entorno para el Sistema de Análisis Microcurricular
# Copiar como .env y completar con tus valores reales
# NUNCA commitear el archivo .env al repositorio

# ── Integración LLM (Opción A) ─────────────────────────────────────────────
# Requerido si CONFIG['LLM_ENABLED'] = True en config.py
ANTHROPIC_API_KEY=sk-ant-...

# Alternativa: OpenAI (no implementado aún — solo 'anthropic' está soportado)
# OPENAI_API_KEY=sk-...
